import 'package:flutter/material.dart';

class LogoMiniOne extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      margin: EdgeInsets.all(16.0),
      height: 40.0,
      child: Image.asset("images/logoPromario.png"),
    );
  }
}
